# macOS
.DS_Store

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk
# JNE build's bibliography is regenerated by ./build.sh jne; the PDF is tracked
# so co-authors can read the manuscript form without a TeX install.
TN0484_jne.bbl

# Python
__pycache__/
*.pyc

# Python venv
.venv/

# Regenerable intermediate data (NOT part of the committed figure-data set;
# run_all.py / the generators recreate these on demand)
code/analyses_v2.npz
code/sweep_hi_stable.npz
code/sweep_hi_cycle.npz
code/tacs_jsweep.npz

# Non-manuscript diagnostic outputs and raster-figure PDF byproducts
figures/fig_lanmm_checker.*
figures/fig_lanmm_arnold_p1.pdf
figures/fig_lanmm_arnold_p2.pdf

# Internal working notes / correspondence (excluded from the public archive)
_private/
